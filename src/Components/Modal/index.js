import React from "react";
// import Style from "./Style.module.css";

export default function Modal(props) {
    return (<div>
        <div>
            {props.children}
        </div>
    </div>)
    
}